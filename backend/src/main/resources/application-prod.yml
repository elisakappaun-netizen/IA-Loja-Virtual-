spring:
  datasource:
    url: ${DATABASE_URL:jdbc:postgresql://localhost:5432/lojavirtual}
    username: ${DATABASE_USER:postgres}
    password: ${DATABASE_PASSWORD:postgres}
    driver-class-name: org.postgresql.Driver
  jpa:
    hibernate:
      ddl-auto: update
    show-sql: false
